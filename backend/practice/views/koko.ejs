<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- <link rel="stylesheet" href="/css/style.css"> -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input name="title" class="w-full px-2 py-1 block outline-none text-black text-xl rounded-md"
                    placeholder="Title" type="text">
                <textarea name="details" class="mt-4 w-72 w-full px-2 py-1 block outline-none text-black rounded-md resize-none"
                    placeholder="Write Your Detail Task Here..."></textarea>
                <input type="submit" class="px-2 py-1 bg-blue-500 rounded-md text-sm mt-5" value="Create Task">
            </form>
        </div>


        <div class="tasks p-10 flex gap-5 flex-wrap">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){%>
                    <div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-tighter "> <%= val %> </h1>
                        <a href="" class="text-blue-500 inline-block mt-3">Read More.</a>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-slate-700 dark:text-slate-500">NO TASK YET</h3>
            <% } %>

        </div>
    </div>

    <script src="/js/script.js"></script>
</body>

</html>